query FetchChannelById($channelId: String!) {
  channel(id: $channelId) {
    id
    version
    key
    roles
    name
    nameAllLocales {
      locale
      value
    }
    description
    descriptionAllLocales {
      locale
      value
    }
    custom {
      type {
        id
        key
      }
      customFieldsRaw {
        name
        value
      }
    }
  }
}

